# Scheduled post example

If you’re reading this, the `publishAt` time has passed (or you opened it with `?preview=1`).

This lets you **commit posts ahead of time** and have them appear automatically based on time.
